apiVersion: infra.contrib.fluxcd.io/v1alpha1
kind: Terraform
metadata:
  name: ec2-setup
  namespace: flux-system
spec:
  interval: 1m
  approvePlan: "auto"
  path: .
  storeReadablePlan: human
  # backendConfig:
  #   disable: true
  # cliConfigSecretRef:
  #   name: tfe-cli-config
  sourceRef:
    kind: GitRepository
    name: flux-system